requests>=2.31.0
websocket-client>=1.8.0
moviepy==1.0.3
Pillow>=10.0.0
numpy>=1.24.0
imageio-ffmpeg>=0.4.9
pytest>=8.0.0
# Optional: best voice quality (heavy). Install when ready:
# TTS>=0.22.0
# Fallback TTS (lightweight, works offline-ish via edge):
edge-tts>=6.1.0
