# Voice samples

Place your clone sample here:

- `default.wav` — 6–15 seconds of clean speech, no music, one speaker

DirectorX uses this with **XTTS v2** when the `TTS` package is installed.

Until then, the pipeline automatically falls back to **edge-tts** (still high quality EN/HI neural voices).
